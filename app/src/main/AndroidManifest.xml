<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baibu.test">

    <uses-permission android:name="android.permission.READ_CONTACTS"></uses-permission>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />


    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MyScanActivity" />
        <activity android:name=".Secondeactivity" />
        <activity android:name=".BeziOneActivity" />
        <activity android:name=".BeziTwoActivity" />
        <activity android:name=".BeziThreeActivity" />
        <activity android:name=".BeziFourActivity" />
        <activity android:name=".PathViewOneActivity" />
        <activity android:name=".PathViewOneMeActivity" />
        <activity android:name=".ZoomImageActivity" />
        <activity android:name=".TagLayoutActivity" />
        <activity android:name=".MyTagLayoutActivity" />
        <activity android:name=".view.MyHuiTanActivity" />

    </application>

</manifest>